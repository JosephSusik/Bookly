version: '3.9'

services:
  db:
    image: postgres:15
    container_name: my_library_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: librarydb
    ports:
      - "5433:5432"  # Avoids clashing with work's port 5432
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:

